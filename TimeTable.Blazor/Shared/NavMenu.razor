<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">TimeTable.Blazor</a>
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>

<div class="@NavMenuCssClass nav-scrollable" @onclick="ToggleNavMenu">
    <nav class="flex-column">
        <div class="nav-item px-2">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                <span class="oi oi-home" aria-hidden="true"></span> Timetable
            </NavLink>
        </div>
        <div class="nav-item px-2">
            <NavLink class="nav-link" href="Students">
                <span class="oi oi-person" aria-hidden="true"></span> Students
            </NavLink>
        </div>
        <div class="nav-item px-2">
            <NavLink class="nav-link" href="Teachers">
                <span class="oi oi-people" aria-hidden="true"></span> Teachers
            </NavLink>
        </div>
        <div class="nav-item px-2">
            <NavLink class="nav-link" href="Subjects">
                <span class="oi oi-book" aria-hidden="true"></span> Subjects
            </NavLink>
        </div>
        <div class="nav-item px-2">
            <NavLink class="nav-link" href="Import">
                <span class="oi oi-plus" aria-hidden="true"></span> Import
            </NavLink>
            <NavLink class="nav-link" href="Export">
                <span class="oi oi-inbox" aria-hidden="true"></span> Export
            </NavLink>

        </div>
    </nav>
</div>

@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
